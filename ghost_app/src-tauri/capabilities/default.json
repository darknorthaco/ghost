{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main GHOST window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default"
  ]
}
